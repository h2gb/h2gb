Does it need to load on startup?
Env var for the directory?
Load dynamically?
Formats? YAML, TOML, CSV, etc? C?
Complexity? Do we want more than just fields?
Bidirectional all the way
* Look up a->b or b->a
